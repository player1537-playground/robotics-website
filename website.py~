from flask import Flask
app = Flask(__name__)

@app.route("/")
@app.route("/<page>")
def show_page(page="index"):
    return render_template(page + '.html')

if __name__ == "__main__":
    app.run(debug=True)

